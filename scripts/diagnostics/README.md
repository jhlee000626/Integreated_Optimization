# Diagnostic Scripts

This directory contains ad hoc utility scripts that were moved out of the
project root to keep the main workspace focused on maintained code.

Notes:
- These scripts are best-effort utilities, not part of the supported runtime.
- Some scripts were written for one-off debugging sessions and may need small
  updates before reuse.
- Maintained manual verification flows live under `tests/`.
